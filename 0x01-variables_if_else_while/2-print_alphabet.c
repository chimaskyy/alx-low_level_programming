#include <stdio.h>

/**
 * main - Entry point of the program
 * Description: Print alphabets in loswecase
 * Return: 0 (success)
 */

int main(void)
{
	char x;
	x = 'a';
		while
			(x <= 'z')
			{
				putchar(x);
				x++;
			}
	putchar('\n');
	return (0);

}
