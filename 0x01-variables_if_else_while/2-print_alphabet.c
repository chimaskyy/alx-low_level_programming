#include <stdio.h>

/**
 * main - Entry point of the program
 * Description 'print alphabets in lowercase'
 * Return: Always 0 (success)
 */

int main(void)
{
	char x;
	x = 'a';
		while
			(x <= 'z')
			{
				putchar(x);
				x++;
			}
	putchar('\n');
	return (0);

}
